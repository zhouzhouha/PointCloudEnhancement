diff --git a/metric/loss.py b/metric/loss.py
index f1983d0..94f5014 100644
--- a/metric/loss.py
+++ b/metric/loss.py
@@ -9,7 +9,10 @@ import numpy as np
 from metric.emd.emd_module import emdFunction
 from metric.weight import Weight
 # from modules.utils.fps import square_distance
-from kaolin.metrics.pointcloud import chamfer_distance
+try:
+    from kaolin.metrics.pointcloud import chamfer_distance
+except ImportError:
+    chamfer_distance = None
 from contextlib import redirect_stdout
 with open(os.devnull, "w") as outer_space, redirect_stdout(outer_space):
     from metric.PyTorchCD.chamfer3D import dist_chamfer_3D
@@ -155,6 +158,8 @@ class ChamferCUDA(nn.Module):
         points1: [B, N, 3]
         points2: [B, N, 3]
         """
+        if chamfer_distance is None:
+            raise RuntimeError("kaolin is required for ChamferCUDA, but not for PD-LTS denoising inference.")
         cost = chamfer_distance(points1, points2)
         return torch.sum(cost)
 
